import 'package:flutter/material.dart';

class QueensListScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('viviane'),
    );
  }
}
